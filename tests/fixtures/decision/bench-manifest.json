{
  "cross_branch": [
    "contexts_schema.request.json",
    "compiled_schema.golden.json"
  ],
  "synthesis_only": [
    "decision_basic.golden.json",
    "decision_basic.shape.json",
    "decision_letter.golden.json"
  ],
  "notes": "Trie fixtures are byte-identical on both branches. Decision fixtures require synthesis; ancestor skips them cleanly (exit 0 SKIP, not FAIL)."
}
